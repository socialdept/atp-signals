{
  "name": "tap-batcher",
  "version": "1.0.0",
  "private": true,
  "description": "Bun proxy that buffers Tap WebSocket events and POSTs them in bulk to Laravel",
  "scripts": {
    "start": "bun run index.ts",
    "test": "bun test"
  }
}
